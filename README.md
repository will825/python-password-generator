# Python Password Generator 🔐

A simple, customizable password generator written in Python and designed to run in the terminal.

## Features

- Choose password length  
- Generate one or multiple passwords  
- Options to include:  
  - Lowercase  
  - Uppercase  
  - Numbers  
  - Symbols  
- Input validation to prevent errors  
- Ensures at least one character from each selected type  
- Easy-to-read output  
- Option to generate more passwords without restarting  

## How to Run

1. Clone the repository:

```bash
git clone https://github.com/will825/python-password-generator.git
cd python-password-generator
